<h1>Showing the Post</h1>
<h2> <%= @post.title %></h2>
<p><%= @post.posting %></p>

<%= link_to "Edit", edit_post_path(@post) %>
<%= link_to "Delete", post_path(@post), method: :delete, data:{confirm: "Are you sure you want to delete it?"} %>
<%= link_to "Home", root_path %>
